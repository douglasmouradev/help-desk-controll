# ⚡ EXECUÇÃO RÁPIDA - Sistema Help Desk Controll IT## 🚀 **EXECUTAR EM 10 MINUTOS**### **1. PREPARAR SERVIDOR (3 min)**```bash# Ubuntu/Debian - Instalar tudo de uma vezsudo apt update && sudo apt upgrade -ysudo apt install apache2 mysql-server php php-mysql php-json php-curl php-mbstring php-openssl php-gd php-zip unzip git -y# Habilitar módulos Apachesudo a2enmod rewrite headers sslsudo systemctl restart apache2sudo systemctl enable apache2 mysql```### **2. CONFIGURAR BANCO (2 min)**```bash# Criar banco e usuáriosudo mysql -u root -p``````sqlCREATE DATABASE helpdesk_titanium CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;CREATE USER 'helpdesk_user'@'localhost' IDENTIFIED BY 'MinhaSenha123!';GRANT ALL PRIVILEGES ON helpdesk_titanium.* TO 'helpdesk_user'@'localhost';FLUSH PRIVILEGES;EXIT;``````bash# Importar bancocd /var/www/htmlsudo git clone https://github.com/seu-usuario/help-desk-Controll IT.git helpdeskcd helpdeskmysql -u helpdesk_user -p helpdesk_titanium < database/database_schema.sql```### **3. CONFIGURAR SISTEMA (2 min)**```bash# Configurar permissõessudo chown -R www-data:www-data /var/www/html/helpdesksudo chmod -R 755 /var/www/html/helpdesksudo mkdir -p /var/www/html/helpdesk/uploads /var/www/html/helpdesk/logssudo chmod -R 777 /var/www/html/helpdesk/uploads /var/www/html/helpdesk/logs# Configurar sistemasudo nano api/config.php```**Alterar estas linhas no arquivo `api/config.php`:**```phpdefine('DB_HOST', 'localhost');define('DB_NAME', 'helpdesk_titanium');define('DB_USER', 'helpdesk_user');define('DB_PASS', 'MinhaSenha123!');define('JWT_SECRET', 'sua_chave_jwt_super_secreta_aqui_123456789');```### **4. CONFIGURAR APACHE (2 min)**```bash# Criar virtual hostsudo nano /etc/apache2/sites-available/helpdesk.conf```**Conteúdo do arquivo:**```apache<VirtualHost *:80>    ServerName localhost    DocumentRoot /var/www/html/helpdesk        <Directory /var/www/html/helpdesk>        AllowOverride All        Require all granted    </Directory>        ErrorLog ${APACHE_LOG_DIR}/helpdesk_error.log    CustomLog ${APACHE_LOG_DIR}/helpdesk_access.log combined</VirtualHost>``````bash# Ativar sitesudo a2ensite helpdesksudo a2dissite 000-defaultsudo systemctl reload apache2```### **5. TESTAR SISTEMA (1 min)**```bash# Acessar no navegador# http://localhost/helpdesk/# Login de teste:# Usuário: admin# Senha: senha123```---## 🔧 **COMANDOS DE MANUTENÇÃO**### **Reiniciar Serviços**```bashsudo systemctl restart apache2 mysql```### **Verificar Status**```bashsudo systemctl status apache2 mysql```### **Verificar Logs**```bashsudo tail -f /var/log/apache2/helpdesk_error.log```### **Verificar Banco**```bashmysql -u helpdesk_user -p helpdesk_titanium -e "SHOW TABLES;"```---## 🚨 **PROBLEMAS COMUNS**### **Erro 500 - Internal Server Error**```bash# Verificar permissõessudo chown -R www-data:www-data /var/www/html/helpdesksudo chmod -R 755 /var/www/html/helpdesk# Verificar logssudo tail -f /var/log/apache2/error.log```### **Erro de Conexão com Banco**```bash# Verificar se MySQL está rodandosudo systemctl status mysql# Testar conexãomysql -u helpdesk_user -p helpdesk_titanium```### **Página em Branco**```bash# Verificar se PHP está funcionandophp -v# Verificar extensões PHPphp -m | grep mysql```---## 📊 **VERIFICAÇÃO RÁPIDA**### **Testar Funcionalidades:**1. ✅ **Acessar:** http://localhost/helpdesk/2. ✅ **Login:** admin / senha1233. ✅ **Criar ticket:** Abrir novo chamado4. ✅ **Visualizar:** Lista de chamados5. ✅ **Editar:** Alterar status/prioridade### **Verificar API:**```bash# Testar login via APIcurl -X POST http://localhost/helpdesk/api/auth.php?action=login \  -H "Content-Type: application/json" \  -d '{"username":"admin","password":"senha123"}'```---## 🔐 **CONFIGURAÇÕES DE SEGURANÇA**### **Antes de Produção:**```bash# 1. Alterar senhas padrão# Editar: database/test_users.sql# 2. Configurar HTTPSsudo apt install certbot python3-certbot-apache -ysudo certbot --apache -d seu-dominio.com# 3. Configurar firewallsudo ufw enablesudo ufw allow 22,80,443/tcp```---## 📁 **ESTRUTURA DE ARQUIVOS IMPORTANTES**```/var/www/html/helpdesk/├── index.html              # Página principal├── script.js               # JavaScript├── api/                    # Backend PHP│   ├── config.php         # Configurações│   ├── auth.php           # Autenticação│   ├── tickets.php        # Gestão de tickets│   └── users.php          # Gestão de usuários├── database/              # Scripts SQL│   └── database_schema.sql└── uploads/               # Arquivos enviados```---## 🎯 **RESULTADO FINAL**Após executar todos os passos:- ✅ Sistema rodando em: http://localhost/helpdesk/- ✅ Login funcionando- ✅ Todas as funcionalidades operacionais- ✅ Banco de dados configurado- ✅ API funcionando**Tempo total: ~10 minutos**---**🎉 Sistema pronto para uso!****Para dúvidas, consulte: `GUIA_HOSPEDAGEM_COMPLETO.md`**