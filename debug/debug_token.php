<?php// Debug do token JWTerror_reporting(E_ALL);ini_set('display_errors', 1);echo "<h1>🔍 Debug do Token JWT</h1>";// Simular headers de autorização$_SERVER['HTTP_AUTHORIZATION'] = 'Bearer ' . ($_GET['token'] ?? 'teste');echo "<h2>1. Headers recebidos:</h2>";echo "Authorization: " . ($_SERVER['HTTP_AUTHORIZATION'] ?? 'Não definido') . "<br>";echo "<h2>2. Testando função getAuthToken():</h2>";require_once 'api/config.php';$token = getAuthToken();echo "Token extraído: " . ($token ? $token : 'Nenhum token encontrado') . "<br>";echo "<h2>3. Testando função requireAuth():</h2>";try {    $user = requireAuth();    echo "✅ Usuário autenticado: " . json_encode($user, JSON_PRETTY_PRINT) . "<br>";} catch (Exception $e) {    echo "❌ Erro na autenticação: " . $e->getMessage() . "<br>";}echo "<h2>4. Testando JWT::decode():</h2>";if ($token) {    try {        $payload = JWT::decode($token);        if ($payload) {            echo "✅ Token válido: " . json_encode($payload, JSON_PRETTY_PRINT) . "<br>";        } else {            echo "❌ Token inválido ou expirado<br>";        }    } catch (Exception $e) {        echo "❌ Erro ao decodificar token: " . $e->getMessage() . "<br>";    }} else {    echo "❌ Nenhum token para testar<br>";}echo "<h2>5. Teste com token real:</h2>";echo "<p>Para testar com um token real, acesse o sistema, faça login e copie o token do localStorage.</p>";echo "<p>Depois acesse: <code>debug_token.php?token=SEU_TOKEN_AQUI</code></p>";echo "<h2>6. Instruções:</h2>";echo "<ol>";echo "<li>Faça login no sistema</li>";echo "<li>Pressione F12 → Application → Local Storage</li>";echo "<li>Copie o valor de 'authToken'</li>";echo "<li>Acesse: debug_token.php?token=SEU_TOKEN</li>";echo "</ol>";?>